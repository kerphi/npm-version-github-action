name: 'CI build-test-push'

on: [push, tags, pull_request, release]

jobs:
  build-test-push:
    name: 'Build Test Push'
    runs-on: ubuntu-latest

    steps:
      - name: 'Checkout source code'
        uses: 'actions/checkout@v2'
